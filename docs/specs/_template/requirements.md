# Requirements — <feature-name>

> 雛形。新規 spec は本ディレクトリ（`_template/`）を `docs/specs/<feature-name>/` にコピーして使う。
> 記法は [`../../steering/conventions.md`](../../steering/conventions.md) の EARS 節に従う。
> **逆生成時**: 各要求に出典 `file:line` を必ず付け、人手レビューで突き合わせること。

## 対象 / スコープ

- **対象モジュール/機能**: `src/<module>.py`（<役割の一行説明>）
- **スコープ内**: <この spec が扱う範囲>
- **スコープ外**: <扱わない範囲>

## 用語集

| 用語 | 定義 |
|:--|:--|
| <Term> | <定義> |

## 要求一覧（EARS）

各要求は一意 ID・EARS 文・出典・対応テストを記す。

| ID | 種別 | 要求（EARS） | 出典 | 対応テスト |
|:--|:--|:--|:--|:--|
| R-XXX-01 | ユビキタス | システムは … すること。 | `src/<module>.py:NN` | `tests/...::test_...` |
| R-XXX-02 | イベント駆動 | … したとき、システムは … すること。 | `src/<module>.py:NN-MM` | — |
| R-XXX-03 | 状態駆動 | … の間、システムは … すること。 | `src/<module>.py:NN` | — |
| R-XXX-04 | 異常系 | … のとき、システムは … すること。 | `src/<module>.py:NN` | — |

## 前提条件 / 不変条件

- <呼び出し側が満たすべき前提（例: reset は全ワーカー停止後のみ）。出典 `file:line`>

## 未確定 / 要レビュー事項

- [ ] <コードから意図が読み取れず推測した点。レビューで確認する>
